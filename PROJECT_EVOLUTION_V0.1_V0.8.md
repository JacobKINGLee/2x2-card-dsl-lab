# 2×2 Card DSL Lab：V0.1–V0.8 对话与项目演进总结

> 更新范围：从最初参考照片中的彩色 2×2 卡片目标，到 V0.8 华为云 openPangu 内容驱动生成。
>
> 文档目的：记录每一轮讨论解决了什么问题、增加了什么能力，以及当前系统中 LLM 与确定性引擎的职责边界。

## 1. 最初目标

项目一开始能够生成卡片，但主要是朴素、固定的样式。导师给出的参考照片包含天气、运动、睡眠、电量、内存、日程等卡片，视觉上具有这些共同特征：

- 160×160vp 左右的小尺寸 2×2 卡片；
- 不同语义场景使用不同色彩和图形表达；
- 标题、图标、核心数值、辅助说明和操作按钮有清晰层级；
- 同一批卡片不完全同构，存在居中、左右分栏、环形指标、进度条等变化；
- 卡片漂亮，但仍需满足文字可读、触控可用和内容不溢出的工程约束。

因此，项目目标从“给固定模板换颜色”逐步转变为：

> 输入任意内容，由系统生成语义合适、布局可靠、视觉多样且可验证的卡片。

## 2. 总体演进路线

```text
V0.1  固定 DSL 与基础渲染
  ↓
V0.2  约束驱动布局
  ↓
V0.3  泛化布局、候选搜索与压力测试
  ↓
V0.4  语义驱动视觉决策
  ↓
V0.5  布局 + 视觉双求解与候选比较
  ↓
V0.6  批量生成与多样性控制
  ↓
V0.7  质量门禁、自动修复与优先级降级
  ↓
V0.8  自然语言 → LLM/本地语义计划 → 卡片 DSL
```

当前完整链路为：

```text
自然语言内容
  → LLM / 本地语义编译器
  → 受约束的语义计划
  → 无坐标 Element DSL
  → 布局候选求解 + 视觉候选求解
  → Quality Gate + Auto Repair
  → Layout / Visual IR
  → 160×160vp 卡片渲染
```

## 3. 每一轮更新内容

### V0.1：从参考图建立可运行的卡片 DSL 实验室

第一版先回答“参考照片中的效果能不能被程序化表达”。完成内容包括：

- 建立 2×2 卡片的 Element DSL；
- 支持标题、图标、核心指标、辅助文本和胶囊按钮；
- 建立天气、睡眠、运动、电量等预设场景；
- 完成 160×160vp 卡片预览、DSL 查看和基本交互；
- 建立统一的颜色、圆角、间距、阴影和字体层级；
- 生成站点图标、社交分享图及对应元数据。

这一版实现了“样式明显比原始卡片丰富”，但布局仍更接近人工编写的模板。

### V0.2：用约束求解替代固定模板

第二轮的重点是让 DSL 描述“元素是什么”，而不是让 DSL 直接写死元素坐标。

新增能力：

- 输入 DSL 中禁止出现 `x / y / width / height`；
- 由布局引擎测量文本并生成 Layout IR；
- 引入间距、边界、对齐、重叠、文本容纳等硬约束；
- 根据卡片元素选择合适的构图方式；
- 输出求解过程、布局坐标和约束检查结果；
- 无法同时容纳全部信息时，按优先级进行压缩或降级。

架构从：

```text
DSL → 直接渲染
```

演进为：

```text
Element DSL → Validator → Constraint Solver → Layout IR → Renderer
```

### V0.3：泛化组件、候选搜索与可重复压力测试

第三轮不再只验证少数手写卡片，而是验证系统能否处理多种组件组合。

新增能力：

- 泛化 `text`、`appIcon`、`heroMetric`、`progressRing`、`miniProgress`、`capsuleButton` 等元素；
- 支持 leading、centered、trailing 等多种构图；
- 同一个 DSL 生成多个布局候选，再按约束和评分选择最佳方案；
- 固定随机种子生成 250 组文本、图片与 1–4 指标组合；
- 汇总求解成功率、平均布局得分、违规率和高频失败原因；
- 增加测试案例浏览器，可查看任意案例的输入 DSL 和 Layout IR；
- 增加单案例 160×160vp 真实渲染预览，而不只是统计数字。

当前固定测试集展示的核心指标包括：求解成功率 96.4%、平均布局得分 94.3/100、违规率 3.6%。

### V0.3 补强：离线本地演示流程

为了避免答辩或演示时依赖公网环境，增加了 Windows 本地演示能力：

- 增加 `start-local-demo.bat`；
- 自动检查依赖、构建并启动本地服务；
- 提供仅本机访问和局域网访问两种脚本入口；
- README 增加完整的离线演示说明；
- 增加技术报告生成工具。

### V0.4：让语义决定视觉，而不只是随机换色

这一轮解决“布局正确，但卡片仍可能像同一模板批量换皮”的问题。

新增 `Visual Resolver`：

- DSL 增加 `domain / state / emphasis` 视觉上下文；
- 根据天气、健康、运动、系统、能源、效率、环境等领域选择色彩族；
- 根据状态选择渐变、柔光、实色等 surface；
- 为语义匹配、对比度、层级、强调程度和克制程度分别评分；
- 计算视觉总分与文字对比度；
- 输出选择该视觉方案的可解释原因。

从这一版开始，颜色不再只是装饰，而是语义表达的一部分。

### V0.5：布局与视觉双求解，展示 Top 3 设计候选

单独挑选最佳布局或最佳配色都可能得到局部最优，因此这一轮把两类候选组合评估。

新增能力：

- 布局引擎产生多个 composition 候选；
- 视觉引擎产生多个 palette / surface 候选；
- 将布局分与视觉分组合成设计总分；
- 过滤对比度不合格的视觉候选；
- 页面展示 Top 3 设计候选，可切换比较；
- 对每个候选显示构图、视觉风格、质量状态和总分。

这使系统从“输出唯一答案”变成“搜索设计空间并选择较优答案”。

### V0.6：批量卡片画廊与多样性控制

参考照片的魅力来自整组卡片共同形成的节奏，而不是单张卡片漂亮。因此这一轮增加批量层面的设计控制。

新增能力：

- 增加语义卡片画廊；
- 同时生成天气、睡眠、运动、电量、日程等多个场景；
- 在批量选择时记录 palette、surface 和二者组合的使用次数；
- 对重复方案施加惩罚，使相邻卡片避免全部使用相同构图或相同背景；
- 汇总画廊中的卡片数、色彩族数和 surface 数量；
- 保持每张卡片独立满足可读性和约束要求。

这一步把“单卡最优”扩展为“整组协调且有差异”。

### V0.7：质量门禁、自动修复与优先级降级

漂亮的候选不能直接渲染，必须先通过产品质量检查。

新增 Quality Gate：

- 正文文字对比度至少 4.5:1；
- 触控区域至少 32×32vp；
- 文字必须适配可用空间；
- 必需元素必须成功放置；
- 元素不能越界或发生不可接受的重叠。

新增 Auto Repair：

- 长文本自动压缩或缩短；
- 触控目标不足时自动扩大；
- 对比度不足时更换前景或视觉候选；
- 空间不足时按元素优先级压缩、隐藏非核心信息；
- 仍无法修复的候选会被拒绝，而不是带问题进入渲染器；
- 页面显示修复时间线、受影响元素和高频拒绝原因。

到这一版，系统具备了“生成—检查—修复—拒绝”的闭环。

### V0.8：让任意自然语言内容真正驱动设计

此前的天气、睡眠、电量等场景主要是预设语义。V0.8 增加自然语言语义编译器，使用户可以输入任意描述。

#### 新增交互

- 增加自然语言输入区域；
- 支持示例内容快速填充；
- 支持 `LLM AUTO` 与 `LOCAL` 两种模式；
- 展示识别出的领域、状态、强调程度、组件类型、置信度和选择理由；
- 显示实际使用的是华为云、OpenAI 还是本地回退。

#### 新增语义计划

模型不能输出 CSS 或坐标，只能返回受 Schema 约束的字段：

- `domain`：内容领域；
- `state`：当前状态；
- `emphasis`：视觉强调级别；
- `title / label / detail`：信息层级；
- `visualization`：heroMetric、progressRing 或 miniProgress；
- `value / unit / progressValue`：核心指标；
- `actionLabel / actionEvent`：交互意图；
- `confidence / rationale`：置信度与解释。

语义计划会经过归一化，再转换为原有 Element DSL，最后继续使用 V0.2–V0.7 已建立的布局、视觉和质量系统。

#### 华为云 openPangu 接入

- 接入华为云 ModelArts MaaS OpenAI 兼容接口；
- 使用 `openpangu-2.0-flash`；
- 使用 Function Calling 返回结构化语义计划；
- 根据 openPangu 的实际约束使用 `tool_choice: required`；
- 修复模型把 `18%` 同时放入 value 和 unit 后产生 `18%%` 的问题；
- API Key 只通过服务端环境变量读取，不进入浏览器代码；
- 云模型超时、网络失败或结果校验失败时，自动回退到确定性本地解析。

#### V0.8 真实联调结果

输入：

```text
手机电量只剩 18%，预计还能使用 2 小时，开启省电模式。
```

得到：

```text
provider       = huawei
model          = openpangu-2.0-flash
domain         = energy
visualization  = miniProgress
displayValue   = 18%
elements       = text / appIcon / miniProgress / capsuleButton
```

## 4. LLM 和系统分别理解什么

V0.8 不是让 LLM 独自设计整张卡片，而是明确分工。

| 环节 | 负责方 | 职责 |
| --- | --- | --- |
| 内容理解 | LLM，失败时本地解析器 | 识别领域、状态、主指标、行动意图和信息层级 |
| 语义约束 | Schema + Normalizer | 限制枚举、长度、数值范围，清理重复单位等异常 |
| 组件选择 | 语义编译器 | 把语义计划转换为允许的卡片组件 |
| 布局设计 | 确定性布局引擎 | 测量文字、枚举构图、求解坐标和选择候选 |
| 视觉设计 | Visual Resolver | 选择语义适配的色彩、surface、前景和强调方式 |
| 产品质量 | Quality Gate | 检查对比度、触控、溢出、越界和必需元素 |
| 修复与降级 | Auto Repair | 修复可修问题，必要时按优先级压缩或隐藏内容 |
| 最终显示 | Renderer | 只渲染通过质量门禁的 Layout / Visual IR |

这样既利用 LLM 对任意内容的理解能力，又避免让模型直接生成不稳定的坐标、CSS 和布局代码。

## 5. 当前主要代码结构

| 文件 | 作用 |
| --- | --- |
| `app/page.tsx` | 工作台、自然语言输入、候选比较、画廊和压力测试浏览器 |
| `app/semantic-interpreter.ts` | 语义计划 Schema、本地解析、归一化和 DSL 编译 |
| `app/api/interpret/route.ts` | 华为云/OpenAI 服务端调用、安全回退和结果验证 |
| `app/layout-engine.ts` | DSL 校验、文本测量、布局候选、质量门禁和自动修复 |
| `app/visual-resolver.ts` | 语义视觉候选、评分、对比度和批量多样性选择 |
| `app/layout-benchmark.ts` | 固定种子的 250 组压力测试与指标汇总 |
| `app/globals.css` | 设计 token、卡片 surface、工作台和响应式样式 |
| `tests/rendered-html.test.mjs` | SSR、固定统计、语义编译、华为云工具调用和输入校验测试 |
| `.env.example` | 模型提供商与服务端环境变量模板，不包含真实密钥 |

## 6. 当前验证状态

- 项目可以完成生产构建；
- ESLint 检查通过；
- 5 项自动测试通过；
- 华为云 openPangu 已完成真实端到端调用；
- 云模型返回的语义计划会经过 Schema、Normalizer 和 DSL Validator 三层校验；
- 未配置密钥或外部网络不可用时，系统仍可以用本地解析器生成卡片。

## 7. 为什么本地链接有时打不开

`127.0.0.1` 和 `localhost` 都表示“当前这台电脑”。它不是永久网站地址，只有本地服务进程正在运行时才能访问。

因此：

- Codex 任务结束、电脑重启或服务进程被回收后，链接会失效；
- `http://127.0.0.1:3002/` 只在本机有效；
- 链接后面不要拼接中文说明，否则中文会被当作 URL 路径；
- 需要长期稳定访问时，应使用已部署的 Sites 公网地址；
- 正式离线演示时，可以使用仓库中的 `start-local-demo.bat` 自行启动服务。

## 8. 后续建议

建议下一阶段按以下顺序推进：

1. **V0.9：稳定交付**——轮换已经在聊天中出现过的 API Key，更新站点 Secret，并发布 V0.8 公网版本；
2. **语义评测集**——准备天气、健康、能源、日程、系统状态和模糊输入等固定样本，统计领域与组件选择正确率；
3. **组件扩展**——增加趋势图、双指标、状态列表、迷你时间轴等组件，但继续禁止 LLM 直接输出坐标；
4. **设计反馈闭环**——允许用户选择候选或标记“不喜欢”，积累对视觉评分权重的反馈；
5. **导师演示模式**——把架构、单卡生成、候选比较、质量修复和批量画廊组织成一条简洁的演示路径。

## 9. 一句话总结

本次对话把项目从“固定模板生成一张比较朴素的卡片”，推进成了一个由自然语言驱动、能够搜索布局与视觉候选、具备质量门禁和安全回退的 2×2 Generative UI 原型。
