# 2×2 卡片组件决策规范 v0.1

## 1. 状态与范围

本文承接已确认的设计原则：**模型理解需求，规则选择组件，应用提供能力，引擎负责渲染。**

本文将该原则展开为组件使用条件、优先级、异常处理和验收案例。v0.1 默认策略已于 2026-09-07 进入实现；旧 Semantic Plan 与历史实验成绩继续保留为 v1 证据，不回写或覆盖。实现采用后置的 `semantic-v0.2 + ux-component-v0.1` 确定性决策层，只有 `ready` 结果进入 DSL、布局与渲染。

现有依据：

- [最初 UX 规范](../2x2_card_UX_design_spec.md)：按钮与应用图标可选；两个并行内容不显示标题区域。
- [布局引擎规范 V3](../2x2_card_constraint_layout_spec_v3.md)：必需内容保留、可选内容降级、尺寸与空间约束。
- [DSL 撰写指南](./CARD_DSL_AUTHORING_GUIDE.md)：当前组件、字段与组合限制。
- [首轮模型实验](./experiments/REPORT.md)：区分事实错误、协议错误和表达规则不符合。

本文只决定“放哪些元素、为什么放”。坐标、尺寸、排版和色彩继续由既有引擎负责。当前 Element DSL 的9类组件和数量限制作为本轮实现边界；扩大能力须单独版本化。

## 2. 输入、职责与规则等级

| 输入 | 主要责任方 | 包含的信息 | 不得代替的信息 |
| --- | --- | --- | --- |
| 内容语义 | 模型 | 事实、数据类型、主体、时间、主次、明确动作意图、否定和展示偏好 | 不发明应用身份、可用动作、图标资源或事件名 |
| 应用上下文 | 宿主应用 | 真实来源、资源、动作注册表、当前可用性、默认主次策略 | 有某个能力不等于用户要求显示它 |
| 产品策略 | 人制定、程序执行 | 内容映射、默认展示、偏好覆盖、信息保留与降级 | 不依靠模型自报置信度证明事实或能力 |
| 渲染约束 | 校验器及引擎 | 组件支持范围、数量、空间、可读性与触控约束 | 布局成功不证明内容正确 |

规则等级：

- **H：拟定硬约束。** 一旦该版本规范获采纳，违反即不得返回正常可交付卡片，例如捏造数值、生成不存在的动作、丢弃用户要求保留的信息。
- **D：拟定默认策略。** 输入未指定时确定性采用；被允许的用户偏好覆盖，不计作事实错误。
- **P：用户明确偏好。** 在事实、应用能力和渲染硬约束范围内优先满足，例如“只用大数字”。

规则优先级：**事实与能力边界、渲染硬约束 → 用户明确要求与禁止 → 已配置场景策略 → 通用默认策略。**

同级冲突不靠生成顺序、confidence或随机选择解决。若能询问用户则澄清，否则返回结构化冲突原因；不能将隐藏关键信息当作成功。用户要求可以覆盖默认展示方式，不能创造不存在的应用能力。

## 3. 决策结果与可解释性

决策器返回以下结果之一。这些是编译接口状态，不是 Element DSL 字段：

| 结果 | 使用条件 | 输出要求 |
| --- | --- | --- |
| `ready` | 事实可表示、要求已满足且布局通过 | DSL及规则命中记录；有可选内容省略时记录省略原因 |
| `needs_clarification` | 存在会改变核心事实或用户必要要求的歧义 | 一个针对歧义的问题和候选含义，不同时返回一张貌似确定的最终卡片 |
| `unsupported` | 已明确的必要需求超出应用或当前组件能力 | 稳定原因码及可用替代方案；未经用户接受不把替代方案当作原需求成功 |
| `no_card` | 非卡片请求，或没有任何可展示内容且未要求展示缺失状态 | 原因码，不补造主指标或操作 |

明确的“心率数据缺失”可以是一条可展示的状态，得到`ready`；它与完全无内容的`no_card`不同。不能读取数据不等于数据为0。

每次决策保留：语义协议版本、UX策略版本、上下文版本、事实来源片段/字段引用、命中规则ID、选中组件、覆盖的默认项、被省略项、冲突原因。记录用于调试和评分，无须全部进入用户卡片。

## 4. 操作入口：先判断是否有动作，再选按钮

### 4.1 最小应用能力上下文

宿主应能提供：已注册动作ID、可识别的语义意图、规范按钮文案、关联主体或目标、必要参数及当前可用性。可选提供已审核的图标按钮资源和适用场景。

例如宿主注册“开启省电”能力，规则将模型提取的动作意图匹配到该能力，再填入宿主提供的`event`。模型不再自由生成`enablePowerSaving`、`enableSaving`等不同事件名。JSON字段合法不表示宿主存在对应处理器。

若尚无真实业务能力，仅供实验，可显式提供模拟能力上下文；结果必须标明为模拟，不声称按钮能执行真实业务。不能将“点击以后通常能跳转”作为隐含能力。

### 4.2 动作决策表

按表中顺序检查。针对一个动作的否定不能扩大为禁止所有动作。

| 规则 | 条件 | 决策 | 等级 |
| --- | --- | --- | --- |
| A01 | “不要按钮／只展示信息”等全局禁止操作入口 | 不生成任何按钮，即使宿主提供动作 | H |
| A02 | 用户仅禁止某动作，但允许/请求其他动作 | 排除被禁止动作，继续检查剩余意图 | H |
| A03 | 只有事实，没有请求动作 | 默认不生成按钮；“电量低”不自动等于“开启省电” | D |
| A04 | 用户明确请求动作，能唯一匹配可用注册能力，参数齐全 | 生成一个对应入口；label、event由注册能力和文案规则提供 | H |
| A05 | 明确请求的动作缺少可确定的目标/参数，或匹配多个能力 | `needs_clarification`；例如“打开详情”但有两个并列主体 | H |
| A06 | 用户必要动作明确，但能力未注册、不可用或上下文未提供 | `unsupported`，原因分别记录未知能力或当前不可用；不伪造可执行按钮 | H |
| A07 | 用户明确“有能力就提供，否则仅展示” | 能力可用则生成，否则省略，并记录原因 | H |
| A08 | 产品希望在无请求时推荐操作 | 仅当宿主显式配置推荐策略且动作确实可用时允许；本版通用默认关闭 | D |
| A09 | 用户要求多个动作，且都不可省略 | 当前每卡最多一个按钮，返回`unsupported`；可提出拆卡方案 | H |
| A10 | 用户明确指定主动作，其他动作允许省略 | 保留唯一主动作，记录其他动作省略；不按模型自报重要性随意丢弃 | H |

缺少注册表时，不得默认“所有自然语言动作均可执行”。A06的正常交付结果不是一张静默删除必要按钮的卡片。交互层可展示说明或预览，但必须保留未满足状态。

### 4.3 胶囊按钮还是图标按钮

| 规则 | 条件 | 决策 | 等级 |
| --- | --- | --- | --- |
| A11 | 动作通过A04，未指定按钮形式 | 默认`capsuleButton`；规范文案说明动作 | D |
| A12 | 用户请求图标按钮，或场景策略默认图标按钮；宿主有该场景已审核且语义明确的图标 | `iconButton`，保留无障碍label和注册event | P / D |
| A13 | “清理内存／开启省电／重新连接”等需要文字消除歧义，尚无图标可理解性证据 | 使用胶囊按钮；不因省空间自动换为图标按钮 | D |
| A14 | 用户必须要纯图标，但缺少已审核资源或语义有歧义 | 澄清是否接受文字按钮；若禁止替代则`unsupported` | H |
| A15 | 空间不足 | 先移除允许省略的装饰/辅助内容；不能把必要动作隐藏或改成未经审核的纯图标 | H |

按钮文字：默认保留动作含义，主题清楚的单主体卡片允许“查看详情”；有两个可能目标时必须消歧。`暂停`不可被简化为`查看详情`。按钮长度与触控检查沿用引擎，缩短文案不得改变动作意图。

按钮点击仍须由宿主根据实时能力状态处理；生成时可用不保证稍后仍然可用。模型或卡片生成流程只提供入口，不因生成按钮自动执行动作。

## 5. 内容主体：先决定信息结构，再映射组件

### 5.1 内容和主次

| 规则 | 条件 | 决策 | 等级 |
| --- | --- | --- | --- |
| C01 | 用户明确主次，例如“突出续航，电量作辅助” | 保留主次；主指标不能被领域默认项替换 | H |
| C02 | 单一可展示指标，没有额外主次问题 | 以该指标为主，进入数据类型映射 | D |
| C03 | 多个并列指标，未指定主次，宿主也没有场景默认 | 默认并列`metric`，不让模型任意挑一个放大 | D |
| C04 | 多指标场景有明确宿主默认主次，且未与用户要求冲突 | 按版本化场景规则设置主次并记录来源 | D |
| C05 | 辅助事实能完整表达为主指标detail或caption | 允许文字表达，保留主体、数值、单位和关系 | D |
| C06 | 用户要求主指标放大且其他指标都独立显示 | 当前DSL禁止主视觉+metric，返回`unsupported`或询问是否接受并列显示 | H |
| C07 | 超过4个必要并列指标，或多个独立卡片意图 | 当前单卡不能无损表示；返回`unsupported`并提出拆卡 | H |
| C08 | 不同主体/时间的数值 | 分别关联主体和时间；不能混合为一个指标或把历史值当当前值 | H |

“无主次的多个指标”不必一律澄清，C03提供确定性默认。只有必需表达发生冲突、无法无损表示或核心事实有歧义时才澄清/返回不支持。

### 5.2 指标组件决策表

先遵循主体结构，再选组件。例如三个并列百分比仍可用三个`metric`，不会因百分比规则生成三个主视觉。

| 规则 | 组件 | 使用条件与默认 | 不使用/覆盖条件 | 信息依据 | 等级 |
| --- | --- | --- | --- | --- | --- |
| C09 | `metric` | 2–4个并列指标，各自有明确标签和值；单项密集展示偏好也允许1个metric | 用户指定单主指标时优先进入主视觉映射；不能与image或主视觉共存 | 指标数量、主次、用户偏好 | D，组合限制H |
| C10 | `heroMetric` | 单核心绝对量、时刻、时长、容量、AQI、文本状态；也支持用户明确只用大数字 | 不替代多个必须并列的信息；非数值内容不强行变成指标 | 数据类型、用户偏好 | D |
| C11 | `miniProgress` | 单核心百分比、完成度、剩余占比，已知其进度语义与尺度 | “只要大数字，不要进度条”可覆盖为heroMetric；绝对值无总量不能猜占比 | 百分比/比例类型、尺度 | D |
| C12 | `progressRing` | 单核心评分，尺度已明确，或宿主注册的环形占比场景 | 评分满分未知且无领域尺度时不画确定比例；明确数字偏好可覆盖 | 评分值、量程、场景策略 | D |
| C13 | 纯文字 | 通知、说明、缺失状态等没有适当主指标的内容 | 不为填字段制造“0”“50%”或虚构状态 | 内容类型和已知/缺失标记 | D，禁止编造H |

进度计算属于确定性规则：

- 显式百分比p：显示值保留真实p，填充值按组件范围限制在0–100。已确认的“超额完成120%”显示120%，填充100%，注明超额；不把事实改写成100%。
- 比例a/b：只有同一指标的分子分母含义已明确且b>0时，计算100×a/b；不得将多个无关数值相除。
- 评分：量程min/max已明确且max>min时按量程换算，显示原始评分及必要的满分信息。不能默认所有“82分”都以100为满分。
- 不可能的测量值与合法超额进度分开处理。电池剩余120%不能只截成100%就视为正常数据，应标记异常并澄清/按宿主无效数据策略处理。
- `%`不独自证明两项数据可比较；剩余电量、降雨概率和湿度保留各自标签。百分比用heroMetric不是事实错误，未遵循默认策略也不自动等于不可用。

## 6. 标题、图标、图片和辅助文字

| 规则 | 对象 | 出现条件 | 不出现/缺失处理 | 信息来源 | 等级 |
| --- | --- | --- | --- | --- | --- |
| V01 | `text/title` | 单主体卡片默认显示短标题，帮助识别主题 | 并列双内容模式沿用原UX省略标题区域；用户要求无标题且各标签能独立识别时允许省略 | 事实主题、宿主名称、用户偏好 | D |
| V02 | `appIcon` | 宿主提供可验证的单一应用身份及可渲染资源，场景策略要求来源标识 | 来源未知、多应用聚合或无资源时省略；不生成虚构“电量服务”身份 | 仅宿主来源上下文 | D，来源真实性H |
| V03 | 内容图标 | 支持图标的内容组件有已审核的语义映射，且图标能帮助识别 | 无映射、无帮助或需节省空间时省略；不强制用sparkles兜底 | 主题/指标类型→注册资源映射 | D |
| V04 | 按钮图标 | 胶囊按钮有可用且有帮助的资源时可添加；iconButton按A12要求 | 胶囊按钮可无图标；不能由任意英文图标名伪造资源 | 已注册动作资源 | D / H |
| V05 | `image` | 图片是用户要求的主体，且宿主提供/验证了可渲染资源与替代文本 | 无资源且图片是必要主体时返回不支持/澄清；不能把文件名当作图片成功 | 用户意图＋资源上下文 | H |
| V06 | `text/body` | 必须保留的说明、通知或适合文字表达的状态 | 无事实内容不补空泛说明 | 输入事实 | D |
| V07 | `detail`或`text/caption` | 有输入支持的辅助事实，且与主体关系明确 | 无内容则省略；必要辅助事实不得因装不下静默删除 | 输入事实、已授权宿主数据 | H |

appIcon回答“来自哪个应用”，内容图标回答“是什么内容”，二者不互相替代。电池图标不能证明卡片来自电池应用。当前appIcon实现主要使用内置语义图标/symbol，与原UX要求的SVG真实应用图标并未完全对齐；真实应用资源需要后续适配，在适配前省略或返回能力限制。

“标题区域可选、Title必选”的旧表述建议解释为：**选择显示标题区域时必须有主标题；卡片整体不强制标题区域。** 此解释为建议，需在UX定稿时同步原文。标题省略不应导致主体、单位或来源无法辨认。

图片与metric、主视觉当前互斥。用户同时要求照片和核心大数值时，不偷偷省略照片，也不凭布局引擎自行突破DSL组合限制。

## 7. 信息不足、异常与偏好冲突

| 规则 | 情况 | 处理 |
| --- | --- | --- |
| E01 | 明确数据缺失，如“心率暂无数据” | 可用文字状态表达；不转为0，不画虚构进度 |
| E02 | 用户要求具体数值但输入无值、宿主也无值 | 澄清或返回无法获取数据；生成模型不能代替数据源 |
| E03 | 同一主体/时间出现相互矛盾的值 | 先识别明确纠正关系；无纠正依据则澄清，不依据置信度选一个 |
| E04 | “原定15:00，改为16:30” | 使用纠正后的16:30，必要时将旧时间作为辅助事实 |
| E05 | 偏好与默认冲突，例如百分比只用大数字 | 满足偏好，记录默认被覆盖；仍需保留%和指标标签 |
| E06 | 偏好与硬约束冲突，例如要求不存在的按钮动作 | 解释不能满足的具体项；接受替代方案前不返回原需求已满足 |
| E07 | 非卡片任务，如写诗 | 返回no_card或交给上层任务路由，不把任意文本塞进核心数值字段 |
| E08 | 内容超过空间，用户要求都保留 | 引擎可调整排版但不能丢失必要事实；仍不可表示则返回unsupported |

用户输入与宿主事实冲突时，先区分“创建用户指定示例”与“展示设备实时状态”。用途不明且影响事实真实性时澄清；不简单设定任一来源永远覆盖另一来源。

## 8. 对照案例：同一输入也会因上下文得到不同卡片

下表预期是组件集合与行为，不是唯一像素布局。未注明真实应用来源时，不生成appIcon。正常展示案例仍需通过现有布局门禁。

| ID | 输入 | 宿主上下文/必要条件 | 预期结果 | 不应出现 |
| --- | --- | --- | --- | --- |
| X01 | 手机电量18% | 无动作推荐策略、来源未知 | 标题＋miniProgress，无按钮 | 自动省电按钮、虚构应用图标 |
| X02 | 手机电量18%，开启省电模式 | 注册且可用的省电能力 | X01＋对应capsuleButton | 模型自由生成event |
| X03 | 同X02 | 没有省电能力上下文 | unsupported，说明必要动作无法验证 | 可执行省电按钮或静默删除动作 |
| X04 | 手机电量18%，只展示，不要按钮 | 即使省电能力可用 | 无按钮 | 推荐动作覆盖用户禁止 |
| X05 | 手机电量18%，不要省电，只查看电池详情 | 电池详情能力可用 | 电池详情按钮 | 省电按钮、把否定扩大为无按钮 |
| X06 | 手机电量83%，只要大数字，不要进度条 | 无额外条件 | heroMetric，value83，unit% | 判为事实错误或强制进度条 |
| X07 | 还可用2小时，剩余电量18%，突出续航 | 无额外条件 | heroMetric显示2小时，辅助文字保留18% | 自动突出电量、丢失辅助电量 |
| X08 | 显示CPU32%、内存4.5GB、磁盘68% | 三项并列 | 三个metric，标签与单位齐全 | 任意选一项主视觉或漏掉两项 |
| X09 | 睡眠82分，满分100 | 量程明确 | progressRing填充82%，显示评分 | 把82当睡眠小时数 |
| X10 | 评分4.2分 | 无满分上下文 | 数字显示4.2分；需环形展示时先补量程 | 猜测满分5或100 |
| X11 | 运动目标完成120% | 合法超额完成 | miniProgress填充100%，显示120%，说明超额 | 修改真实完成度为100% |
| X12 | 设备心率暂无数据 | 无可用测量值 | 文字缺失状态 | 心率0、默认进度50% |
| X13 | 电量显示18%又显示82%，不确定哪个对 | 同主体、无时序依据 | needs_clarification | 擅自确定18% |
| X14 | 会议从15:00改到16:30 | 无额外条件 | heroMetric显示16:30 | 显示旧时间15:00 |
| X15 | 暂停当前播放，用图标按钮 | 已注册暂停能力及该场景审核过的图标 | iconButton，保留无障碍label | 没有处理器的pause事件 |
| X16 | 清理内存 | 注册清理能力，未审核纯图标方案 | capsuleButton，文案明确清理 | 为省空间用含义不明的图标 |
| X17 | 展示这张照片 | 有可用图片资源及替代文本 | image主体 | 用文件名heroMetric冒充照片 |
| X18 | 展示这张照片和三个必须保留的指标 | 两类资源均存在 | 当前DSL组合不支持，提出拆卡 | 静默省略图片或指标 |
| X19 | 同X01 | 已知单一应用身份、资源适配完成、来源展示策略开启 | X01＋真实appIcon | 用battery语义图标冒充应用身份 |
| X20 | 同时展示两个应用的数据 | 多来源 | 并列内容，不生成单一appIcon | 暗示二者都来自某一个应用 |
| X21 | 气温零下5摄氏度 | 无额外条件 | heroMetric，规范值-5，显示可为−5°C | 把等价负数记为事实错误 |
| X22 | 写一首秋天的诗 | 非卡片任务 | no_card/上层任务路由 | 将诗句强行塞入value |

X02/X03、X01/X19必须成对验收，以证明组件选择不是仅由自然语言决定。X04/X05验证否定范围；X06验证用户偏好可以覆盖默认展示。

## 9. 下一版语义协议需要表达什么

模型输出建议覆盖以下概念，先校验案例再定JSON Schema：

| 概念 | 必须能表达 | 应由程序/宿主补充，模型不生成 |
| --- | --- | --- |
| 内容项 | 主体、事实值、单位、数据类型、时间、已知/缺失状态、来源依据 | 图标资源和CSS |
| 信息关系 | 并列、主要/辅助、纠正前后、明确要求保留 | 具体组件名和布局坐标 |
| 动作意图 | 未提及、明确无动作、请求动作、特定动作禁止；动作目标和参数 | event及动作可用性 |
| 展示偏好 | 大数字/进度形式、是否要标题、文字/图标按钮、要求还是可选偏好 | 固定尺寸与颜色 |
| 歧义 | 相互冲突事实、缺少必要目标或尺度 | 以confidence阈值替代明确冲突判断 |

`未提及动作`与`明确不要动作`必须分开，不能都用一个none后再丢失用户禁止信息。评分的量程、比例的分母和多项指标也必须可表示，不能继续只有一个value槽位。

组件选择器输入为“语义结果＋宿主上下文＋版本化策略”，输出为第3节决策结果；只有ready进入DSL Validator、Layout Solver、Visual Resolver、Quality Gate和Renderer。

## 10. 评测对齐

保留首轮54.2%及全部原始记录，新评分另起版本；不覆盖历史成绩。先冻结规范、上下文与等价规则，再用新输入盲测。

| 层次 | 核验内容 | 不混入的内容 |
| --- | --- | --- |
| 事实忠实度 | 值、单位、主体、时间、否定/纠正、必要事实保留；拒绝编造 | 默认组件偏好、主题词是否逐字出现 |
| 协议与能力可执行性 | 原始结构合法、组件组合合法、动作来自注册表且目标正确、资源真实 | 仅靠事件名符合正则就算动作正确 |
| UX要求符合度 | 必须项满足、用户偏好满足、默认策略命中分别报告 | 把允许的偏好覆盖计为违规 |
| 最终渲染质量 | 引擎门禁、真实字体/屏幕验证、必要信息不丢失 | 用服务端估算门禁代表真实视觉QA |

非ready样本另测决策正确性：该澄清时有没有澄清、该不支持时有没有说明。不能把所有非ready都当失败，也不能通过拒绝所有输入取得高分；同时报告可支持输入的出卡覆盖率。

等价规则在评分前统一：Unicode负号与中文“零下”、明确等价单位、时刻表示；不能扩大成忽略单位或猜测时区。“查看详情”在唯一明确目标时允许；多个合理领域分类应预先列集合或降低为偏好项，不能看完输出后临时放宽。

若保留联合指标，建议只联合事实必需项、协议/能力硬约束、用户必要要求及渲染门禁。默认策略命中率单列，并同时公布原始结构通过率与程序修复率。

## 11. 已采纳的 v0.1 默认项与实施顺序

以下默认项作为 v0.1 产品规则执行；后续变更需要提升策略版本并保留原测试证据：

1. 无动作请求时默认无按钮，产品推荐动作默认关闭。
2. 有可用动作时默认胶囊按钮；图标按钮须有明确场景审核依据。
3. 百分比默认进度条、已知量程评分默认圆环，但允许用户明确选择大数字。
4. 无主次的多个指标默认并列metric；不让模型自行挑选并丢弃必要内容。
5. appIcon仅表示真实应用来源；未知或多来源省略。
6. 单主体默认标题，双并列内容可省略标题区域；“Title必选”限定在标题区域存在时。

实施顺序：

1. 审核本文规则和X01–X22预期组件；优先确认按钮与能力上下文。
2. 将确认项同步回UX规范和DSL撰写指南，标注硬规则、默认项和允许覆盖。
3. 定义语义协议与能力上下文Schema，保留旧版本；实现规则选择器和决策追踪。
4. 将对照案例变成无需调用模型的确定性编译测试，再验证布局可实现性。
5. 修订评分等价表达与分层指标，使用独立新输入跑Qwen；后续再安排目标设备实验。

实施前的主要差距是：Semantic Plan 强制单主视觉、图标、按钮及事件；`planToDSL` 固定加入标题、appIcon 和 capsuleButton；缺少能力注册表、无动作、多指标与澄清结果。v0.1 通过后置决策层补齐这些能力，同时保留旧接口用于历史实验复现。

## 12. v0.1 实现记录

- `app/semantic-protocol.ts` 定义 `SemanticDocument 2.0`；模型输出事实、数据类型、主次、否定/纠正、动作语义与展示偏好，不输出组件或可执行事件。
- `app/component-decision.ts` 只接收结构化语义和宿主上下文，实现四态决策、动作注册表匹配、组件选择和规则追踪；函数签名不接受原始自然语言。
- `/api/interpret` 接受可选 `context`；只有通过 DSL、布局、质量以及必要内容无丢失门禁的结果才返回 `ready` 和 DSL。
- 宿主应用与图片资源需要资源 ID 和 `verified` 结果；格式校验与资源可用性分开。
- `tests/component-decision.test.mjs` 将 X01–X22 固化为无需调用模型的确定性验收，并对所有 `ready` 结果执行 DSL 与布局门禁。
- `experiments/scoring.mjs` 分别统计模型理解、规则决策和端到端结果；旧 `semanticPlanSchema`、`planToDSL`、`scoring-v1.mjs` 和实验结果保留用于历史复核。
